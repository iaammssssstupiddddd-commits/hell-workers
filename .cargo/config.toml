[build]
target-dir = "target"

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[alias]
visual-test = "run -p visual_test"

[env]
# CARGO_MANIFEST_DIR が crates/bevy_app/ になるため、
# Bevy のアセット検索パスをワークスペースルートに固定する
BEVY_ASSET_ROOT = { value = ".", relative = true }
